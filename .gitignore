cat > .gitignore << 'EOF'
# node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# build
dist
.vite

# env (ключи и локальные настройки)
.env
.env.local
.env.*.local

# OS
.DS_Store

# editor
.vscode
.idea

# server runtime data (если появится)
# server runtime data (НЕ коммитим содержимое)
src/server/data/*
!src/server/data/.gitkeep
EOF
